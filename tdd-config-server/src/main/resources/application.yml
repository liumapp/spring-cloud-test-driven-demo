spring:
  application:
    name: tdd-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/liumapp/spring-cloud-test-driven-demo/
          search-paths: config

server:
  port: 7001

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1234/eureka/

